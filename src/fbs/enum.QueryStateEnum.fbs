/**
 * @author Artem Lytvynov
 * @copyright Artem Lytvynov
 * @license Apache2.0
 */

namespace enum;

/// Query states enum.
enum QueryStateEnum: byte {
  PROCESSING,
  SCHEMA,
  CHUNK,
  DONE,
  FAIL,
}