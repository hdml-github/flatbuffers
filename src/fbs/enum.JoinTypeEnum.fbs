/**
 * @author Artem Lytvynov
 * @copyright Artem Lytvynov
 * @license Apache2.0
 */

namespace enum;

/// Join types enum.
enum JoinTypeEnum: byte {
  Cross,
  Inner,
  Full,
  Left,
  Right,
  FullOuter,
  LeftOuter,
  RightOuter,
}