/**
 * @author Artem Lytvynov
 * @copyright Artem Lytvynov
 * @license Apache2.0
 */

namespace enum;

/// Dataset states enum.
enum DatasetStateEnum: byte {
  PROCESSING,
  SCHEMA,
  CHUNK,
  DONE,
  FAIL,
}